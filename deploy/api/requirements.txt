# Deployment manifest for the example API service.
#
# Pinned to current patched releases. A deploy manifest should be pinned for
# reproducibility, and floor specifiers (>=X) were being resolved by vulnerability
# scanners (OSV / OpenSSF Scorecard) to their *minimum* — pulling in long-superseded
# transformers/torch versions with known advisories. These pins update the demo
# stack and clear the scan. The library itself (see pyproject.toml) depends only on
# numpy + sentence-transformers; installing `groundlens` does not pull these pins.
groundlens>=2026.4.0
fastapi==0.138.1
uvicorn[standard]==0.49.0
transformers==5.12.1
torch==2.12.1
