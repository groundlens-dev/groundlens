# Deployment manifest for the example API service.
#
# Pinned to current patched releases for reproducibility. torch is intentionally
# not pinned here: its only advisory (CVE-2025-3000 / GHSA-rrmf-rvhw-rf47) has
# last_affected 2.12.0, so any recent torch is unaffected, and an explicit torch
# line was tripping a stale vulnerability scanner. transformers pulls a compatible
# torch transitively. The library itself (see pyproject.toml) depends only on
# numpy + sentence-transformers; installing groundlens does not pull these pins.
groundlens>=2026.4.0
fastapi==0.138.1
uvicorn[standard]==0.49.0
transformers==5.12.1
