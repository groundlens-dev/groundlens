# Documentation build dependencies. The GroundLens package itself is NOT
# installed here: the compiled extension is mocked in conf.py, so the build
# needs no Rust toolchain and no model bundle.
sphinx>=7,<9
furo>=2024.8
myst-parser>=3
sphinx-copybutton>=0.5
sphinx-autodoc-typehints>=2
linkify-it-py>=2
